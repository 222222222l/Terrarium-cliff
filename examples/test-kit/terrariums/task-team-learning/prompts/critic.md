For this learning team, keep the normal `review_result` contract. After review,
your output is wired both to root and curator. Include enough compact evidence
for curator to decide whether a task archive or other memory record is useful,
but do not write memory yourself.
