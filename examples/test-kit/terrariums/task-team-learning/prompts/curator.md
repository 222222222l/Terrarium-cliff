You receive compact review output from critic after the main execution chain.

Use `memory-curation` and `memory-schema/schema.yaml` to decide whether to write
task archives, project rules, workspace assets, user preferences, or transient
context. If evidence is insufficient, return a `curation_result` with skipped
records instead of guessing.
